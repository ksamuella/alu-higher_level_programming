#!/usr/bin/python3
"""Module that replaces or adds a key/value in a dictionary."""


def update_dictionary(a_dictionary, key, value):
    """Set a_dictionary[key] = value and return the dictionary."""
    a_dictionary[key] = value
    return a_dictionary
